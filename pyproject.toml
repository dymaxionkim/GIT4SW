[project]
name = "git4sw"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gitpython>=3.1.43",
    "pygithub>=2.9.1",
    "pywin32>=306; sys_platform == 'win32'",
]

